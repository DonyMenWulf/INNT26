import SwipeScreen from './screens/SwipeScreen';

// App.js er lige nu meget simpel, fordi vi kun har byggeet hovedsiden (swipe-siden) endnu.
// Når vi tilføjer flere sider (fx profiloprettelse, matches osv.), sætter vi
// navigation op her (Tab.Navigator), ligesom i 02_Navigation og 04_Styling øvelserne.
export default function App() {
  return <SwipeScreen />;
}
